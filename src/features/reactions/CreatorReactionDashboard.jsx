import { EmptyState } from "../../components/shared/EmptyState";

export function CreatorReactionDashboard() {
  return (
    <EmptyState
      title="Reaction dashboard"
      message="Creator stats will live here."
    />
  );
}
