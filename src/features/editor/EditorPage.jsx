import { EmptyState } from "../../components/shared/EmptyState";

export function EditorPage() {
  return (
    <EmptyState
      title="Meme editor"
      message="Canvas editing starts after the preview picker."
    />
  );
}
